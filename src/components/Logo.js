import { Link } from "react-router-dom";
import LogoImage from "../images/logo.png";

const Logo = () => {
  return (
    <div>
      <img src={LogoImage} alt="Logo" />
    </div>
  );
};

export default Logo;
